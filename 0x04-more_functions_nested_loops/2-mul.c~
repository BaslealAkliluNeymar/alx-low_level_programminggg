#include "main.h"

/**
* mul - multiplies two numbers
* @a - multiplicand
* @b - multiplier
* @c: the result of the multiplication
* Return: the c value
*/
int mul(int a, int b)
{
int c;
c = a * b;
return (c);
}
